{
  "indexes": [
    {
      "//": "ContentService.projects() — publishedQuery appends status=='published' to an orderBy('order'). An equality filter combined with an orderBy on a different field needs a composite index; without it Firestore fails the query at runtime with a console link, which would look like 'the site has no projects'.",
      "collectionGroup": "projects",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "order", "order": "ASCENDING" }
      ]
    },
    {
      "//": "ContentService.featuredProjects() — Home's Featured Work section (02 §4). Two equality filters plus the same ordering.",
      "collectionGroup": "projects",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "featuredOnHome", "order": "ASCENDING" },
        { "fieldPath": "status", "order": "ASCENDING" },
        { "fieldPath": "order", "order": "ASCENDING" }
      ]
    },
    {
      "//": "ContentService.media() — orderBy('order') inside the per-project media subcollection (06 §3.1).",
      "collectionGroup": "media",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "order", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
